```html
<span class="pf-tag-pill">{LABEL}</span>

<style>
  /*
    Small charcoal rectangle with peach text. The only inverted text-on-charcoal
    element that is not a full card — used as a category label anchored to the
    bottom-left of an image frame, or as a tight inline tag inside a card. Sharp
    corners (no radius); 0.75rem weight 600 in Space Grotesk.
  */
  .pf-tag-pill {
    display: inline-block;
    background: var(--ink);
    color: var(--anchor-peach);
    padding: 0.4rem 0.8rem;
    font-family: "Space Grotesk", sans-serif;
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: 0.05em;
  }
</style>
```
