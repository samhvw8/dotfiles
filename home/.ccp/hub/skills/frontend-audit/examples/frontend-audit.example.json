{
	"_doc": "Drop this file at the project root as .frontend-audit.json. The skill scripts read it on every invocation to know where the dev server lives and where to find goal images. Region descriptors are stored per-PNG as sibling files (e.g. design/<name>.regions.json) and auto-generated by the assistant — no entry needed here.",

	"devUrl": "http://localhost:3000",
	"designDir": "design"
}
